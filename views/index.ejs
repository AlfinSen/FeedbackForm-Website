<%- include('layout', { body: ` <div class="form-container">
    <h1>We Value Your Feedback</h1>
    <p class="subtitle">Please let us know your thoughts below.</p>
    <form action="/submit" method="POST">
        <input type="hidden" name="id" value="${typeof id !== 'undefined' ? id : ''}">
        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required placeholder="Your Name"
                value="${typeof name !== 'undefined' ? name : ''}">
        </div>
        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required placeholder="name@example.com"
                value="${typeof email !== 'undefined' ? email : ''}">
        </div>
        <div class="form-group">
            <label for="rating">Rating</label>
            <div class="star-rating">
                <input type="radio" id="star5" name="rating" value="5" required ${(typeof rating !=='undefined' &&
                    rating==5) ? 'checked' : '' } /><label for="star5" title="5 stars">&#9733;</label>
                <input type="radio" id="star4" name="rating" value="4" ${(typeof rating !=='undefined' && rating==4)
                    ? 'checked' : '' } /><label for="star4" title="4 stars">&#9733;</label>
                <input type="radio" id="star3" name="rating" value="3" ${(typeof rating !=='undefined' && rating==3)
                    ? 'checked' : '' } /><label for="star3" title="3 stars">&#9733;</label>
                <input type="radio" id="star2" name="rating" value="2" ${(typeof rating !=='undefined' && rating==2)
                    ? 'checked' : '' } /><label for="star2" title="2 stars">&#9733;</label>
                <input type="radio" id="star1" name="rating" value="1" ${(typeof rating !=='undefined' && rating==1)
                    ? 'checked' : '' } /><label for="star1" title="1 star">&#9733;</label>
            </div>
        </div>
        <div class="form-group">
            <label for="comments">Comments</label>
            <textarea id="comments" name="comments" rows="4" required
                placeholder="Your feedback...">${typeof comments !== 'undefined' ? comments : ''}</textarea>
        </div>
        <button type="submit" class="submit-btn">
            ${(typeof id !== 'undefined' && id) ? 'Update Feedback' : 'Submit Feedback'}
        </button>
    </form>
    </div>
    ` }) %>